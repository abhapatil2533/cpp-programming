#include<iostream>
using namespace std;
int main()
{
    bool isMale= true;
    bool istall= true;
    if(isMale && istall)             // ||for or, && is and, logic gates rules work
    cout<<"You are a tall male";
    else
    cout<<"You are not male";
    return 0;
}
